{
  "buildCommand": "cd client && pnpm install && pnpm build",
  "installCommand": "pnpm install --filter client",
  "framework": "nextjs",
  "outputDirectory": "client/.next",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD client/"
}

